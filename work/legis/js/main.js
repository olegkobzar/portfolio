function preloader() {	if ($('#preloader').length) {		setTimeout(function() {			$('#preloader').fadeOut('slow', function() {				$('body').removeClass('overflow-hidden').css('padding', '');			});		}, 1000);	}}$(window).on('load', preloader);//---------------------------------------//// Подключаем набор написаных скриптов (requare/developRequare.js)//---------------------------------------//function autoFooter() {  if ($('.wrapper').length) {    var height = $('.footer').innerHeight();    $('.wrapper').css('padding-bottom', height).css('margin-bottom', -height);  }}// выполняем их после того, как DOM построится $(document).ready(function() {	 $.reject({  	//	header: 'Your browser is not supported here', // Header Text  	//	paragraph1: 'You are currently using an unsupported browser', // Paragraph 1  	//	paragraph2: 'Please install one of the many optional browsers below to proceed',  	//	closeMessage: 'Close this window at your own demise!' // Message below close window link  		 reject: { 			all: false,			msie: 9,			chrome: 40,			firefox: 30,			safari4: 4		}, 		display: [], // What browsers to display and their order (default set below)		browserShow: true, // Should the browser options be shown?		browserInfo: { // Settings for which browsers to display			chrome: {				// Text below the icon				text: 'Google Chrome',				// URL For icon/text link				url: 'http://www.google.com/chrome/'				// (Optional) Use "allow" to customized when to show this option				// Example: to show chrome only for IE users				// allow: { all: false, msie: true }			},			firefox: {				text: 'Mozilla Firefox',				url: 'http://www.mozilla.com/firefox/'			},			safari: {				text: 'Safari',				url: 'http://www.apple.com/safari/download/'			},			opera: {				text: 'Opera',				url: 'http://www.opera.com/download/'			},			msie: {				text: 'Microsoft Edge',				url: 'http://www.microsoft.com/'			}		},		// Pop-up Window Text		header: 'Did you know that your Internet Browser is out of date?',		paragraph1: 'Your browser is out of date, and may not be compatible with '+					'our website. A list of the most popular web browsers can be '+					'found below.',		paragraph2: 'Just click on the icons to get to the download page',		// Allow closing of window		close: true,		// Message displayed below closing link		closeMessage: 'By closing this window you acknowledge that your experience '+						'on this website may be degraded',		closeLink: 'Close This Window',		closeURL: '#',		// Allows closing of window with esc key		closeESC: true,		// Use cookies to remmember if window was closed previously?		closeCookie: false,		// Cookie settings are only used if closeCookie is true		cookieSettings: {			// Path for the cookie to be saved on			// Should be root domain in most cases			path: '/',			// Expiration Date (in seconds)			// 0 (default) means it ends with the current session			expires: 0		},		// Path where images are located		imagePath: './img/browsers/',		// Background color for overlay		overlayBgColor: '#000',		// Background transparency (0-1)		overlayOpacity: 0.8,		// Fade in time on open ('slow','medium','fast' or integer in ms)		fadeInTime: 'fast',		// Fade out time on close ('slow','medium','fast' or integer in ms)		fadeOutTime: 'fast',		// Google Analytics Link Tracking (Optional)		// Set to true to enable		// Note: Analytics tracking code must be added separately		analytics: false	}); 	//---------------------------------------//	// Подключаем скрипты компонентов страниц ( modules )	//---------------------------------------//	function sliderWrap() {		var wContainer = $('.js-advantages-wrap').innerWidth(),		wWindow = $('body').innerWidth();		$('.js-advantages-slider').css('margin-right', - (wWindow - wContainer) / 2);	}	sliderWrap();	function advantagesSlider() {	  $('.js-advantages-slider').slick({	    // arrows: false,	    infinite: true,	    variableWidth: true	  })	}	$(window).on('load', function() {		setTimeout(function() {			advantagesSlider();		}, 1000)	})	function tabs() {		var $wrap = $('.js-tabs-wrap');		if ($wrap.length) {			$wrap.each(function () {				var $that = $(this),				$tab = $that.find('.js-tab'),				$content = $that.find('.js-tab-content');				$tab.on('click', function (e) {					e.preventDefault();					var self = $(this),					index = self.index();					self.addClass('active').siblings().removeClass('active');					$content.eq(index).addClass('active').siblings().removeClass('active');				});			});		}	}	tabs();
	function firstSlider() {	  var flag = false;	  function init() {	    console.log(window.innerWidth)	     if (window.innerWidth < 768) {	      if (flag) {        	        if ($('.js-first-slider').hasClass('slick-initialized')) {	          $('.js-first-slider').slick('destroy');	          flag = false;	        }        	      }	    } else {	      if(!flag) {	        $('.js-first-slider').slick({	          slidesToShow: 5,	          vertical: true,	          verticalSwiping: true,	          responsive: [	            {	              breakpoint: 951,	              settings: {	                slidesToShow: 2,	                rows: 2	              }	            }	          ]	        });	        flag = true;	      }	    }	  }	  init();	  $(window).on('resize', init)	}	firstSlider();
	// open search	function footerSearch() {	  $('.js-footer-search-wrap').on('click', function() {	    $('.js-footer-search').addClass('active');	  });	}	footerSearch();
	function headerFixed() {	  var headerHeight = $('.header').innerHeight(),	    header = headerHeight + 100,	    scroll = $(window).scrollTop();	  $('.header').addClass('fixed');	  $('.wrapper').css({	    'padding-top': headerHeight	  });	}	headerFixed();	$(window).on('resize', function () {	  headerFixed();	});	function scrollClass() {	  $(window).on('scroll', function () {	    var scroll = $(window).scrollTop(),	      header = $('.header').scrollTop();	    if (scroll > header) {	      $('.header').addClass('active');	    } else if ($('.js-menu-collapse').hasClass('show') && scroll == header) {	    } else if (scroll == header) {	      $('.header').removeClass('active');	    }	  });	}	scrollClass();	function mobileMenu() {	    var scroll = $(window).scrollTop(),	      header = $('.header').scrollTop();	    $('.js-menu-toggler').on('click', function () {	      $(this).toggleClass('active');	      $('.js-menu-collapse').toggleClass('show');	      if (!$('.header').hasClass('active') || (scroll > header)) {	        $('.header').toggleClass('active');	      } else if ($('.header').hasClass('active') && scroll == header) {	        $('.header').removeClass('active');	      }	    });	}	mobileMenu();	$(window).on('resize', function() {	  if ($(window).innerWidth() > 767) {	    $('.header').removeClass('active');	    $('.js-menu-collapse').removeClass('show');	    $('.js-menu-toggler').removeClass('active');	  }	});
	function showInfo() {	  $('.js-info-btn').on('click', function() {	    $('.js-info-collapse').slideToggle('slow');	    $('.info__question-txt--last').toggleClass('active');	  });	}	showInfo();
	$('.js-nav-scroll').mCustomScrollbar({		axis: 'y'	});	function showFullNav() {		$('.js-nav').hover(			function() {				$(this).addClass('active');			},			function() {				$(this).removeClass('active');			}		)		$('.js-nav-btn').on('click', function() {			$(this).toggleClass('active');			$('.js-nav').toggleClass('active-desk');		})	}	showFullNav();	$('.js-select-custom').each(function() {		$(this).selectmenu({			appendTo: '.js-wrap-select'		}) 	})
	function widthSlider() {		var wContainer = $('.js-news-wrap').innerWidth(),		wWindow = $('body').innerWidth();		$('.js-news-slider').css('margin-right', - (wWindow - wContainer) / 2);	}	widthSlider();	$('.js-news-slider').slick({		arrows: true,		infinite: true,		variableWidth: true	})	$(window).on('resize', function() {		widthSlider();	})	function marginWrap() {		var wContainer = $('.js-object-wrap').innerWidth(),		wWindow = $('body').innerWidth();		$('.js-scroll-wrap').css('margin-right', - (wWindow - wContainer) / 2);	}	marginWrap();	$(window).on('resize', function() {		marginWrap();		scrollCard();	})	function scrollCard() {		$('.js-scroll-wrap').mCustomScrollbar({			horizontalScroll: true,			mouseWheelPixels: 233,			live: 'on',			scrollButtons: {				enable: true,				scrollAmount: 233,				scrollType: "pixels"			}		});	}	scrollCard();	//---------------------------------------//	// Подключаем основные скрипты ( develop )	//---------------------------------------//	function customSelect() {	  $('.js-select').selectmenu().selectmenu('menuWidget').addClass('overflow');	}	customSelect();	function phoneMask() {		$('.js-phone-input').inputmask({			mask: "+7 (999) 999-99-99"		});	} 	phoneMask();	function height() {	  if ($('.js-height').length) {	    $('.js-height').matchHeight({	      byRow: true,	      property: 'height',	      target: null,	      remove: false	    });	  }	}	height();    autoFooter();})$(window).on('resize', function() {  autoFooter();})