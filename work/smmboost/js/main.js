function preloader() {	if ($('#preloader').length) {		setTimeout(function() {			$('#preloader').fadeOut('slow', function() {				$('body').removeClass('overflow-hidden').css('padding', '');			});		}, 1000);	}}$(window).on('load', preloader);//---------------------------------------//// Подключаем набор написаных скриптов (requare/developRequare.js)//---------------------------------------//// выполняем их после того, как DOM построится $(document).ready(function() {	 $.reject({  	//	header: 'Your browser is not supported here', // Header Text  	//	paragraph1: 'You are currently using an unsupported browser', // Paragraph 1  	//	paragraph2: 'Please install one of the many optional browsers below to proceed',  	//	closeMessage: 'Close this window at your own demise!' // Message below close window link  		 reject: { 			all: false,			msie: 9,			chrome: 40,			firefox: 30,			safari4: 4		}, 		display: [], // What browsers to display and their order (default set below)		browserShow: true, // Should the browser options be shown?		browserInfo: { // Settings for which browsers to display			chrome: {				// Text below the icon				text: 'Google Chrome',				// URL For icon/text link				url: 'http://www.google.com/chrome/'				// (Optional) Use "allow" to customized when to show this option				// Example: to show chrome only for IE users				// allow: { all: false, msie: true }			},			firefox: {				text: 'Mozilla Firefox',				url: 'http://www.mozilla.com/firefox/'			},			safari: {				text: 'Safari',				url: 'http://www.apple.com/safari/download/'			},			opera: {				text: 'Opera',				url: 'http://www.opera.com/download/'			},			msie: {				text: 'Microsoft Edge',				url: 'http://www.microsoft.com/'			}		},		// Pop-up Window Text		header: 'Did you know that your Internet Browser is out of date?',		paragraph1: 'Your browser is out of date, and may not be compatible with '+					'our website. A list of the most popular web browsers can be '+					'found below.',		paragraph2: 'Just click on the icons to get to the download page',		// Allow closing of window		close: true,		// Message displayed below closing link		closeMessage: 'By closing this window you acknowledge that your experience '+						'on this website may be degraded',		closeLink: 'Close This Window',		closeURL: '#',		// Allows closing of window with esc key		closeESC: true,		// Use cookies to remmember if window was closed previously?		closeCookie: false,		// Cookie settings are only used if closeCookie is true		cookieSettings: {			// Path for the cookie to be saved on			// Should be root domain in most cases			path: '/',			// Expiration Date (in seconds)			// 0 (default) means it ends with the current session			expires: 0		},		// Path where images are located		imagePath: './img/browsers/',		// Background color for overlay		overlayBgColor: '#000',		// Background transparency (0-1)		overlayOpacity: 0.8,		// Fade in time on open ('slow','medium','fast' or integer in ms)		fadeInTime: 'fast',		// Fade out time on close ('slow','medium','fast' or integer in ms)		fadeOutTime: 'fast',		// Google Analytics Link Tracking (Optional)		// Set to true to enable		// Note: Analytics tracking code must be added separately		analytics: false	}); 	//---------------------------------------//	// Подключаем скрипты компонентов страниц ( modules )	//---------------------------------------//	function filterDrop(){	  if ($('.js-filter-btn').length) {	    var btn = $('.js-filter-btn'),	      	content = $('.js-filter-content'),	        text = $('.js-filter-txt');	    btn.on('click', function (e) {	      e.preventDefault();	      $(this).closest('.js-filter-wrap').find(content).slideToggle();	      $(this).toggleClass('active');	      if($(this).hasClass('active')){	      	text.text('Скрыть фильтр');	      }	      else {	      	text.text('Показать фильтр');	      }	    });	  }	}	filterDrop();
	function focusLabel() {		$('.label').each(function() {			$(this).find('input').on('focus', function() {				$(this).closest('.label').addClass('active');			})			$(this).find('input').on('focusout', function() {				$(this).closest('.label').removeClass('active');			})		})	}	focusLabel();
	function profileSorting() {	  $('.js-profile-sorting').tablesorter({	    headers: {	      0: {	        sorter: false	      }	    }	  });	}	profileSorting();
	function sidebarSubmenu() {	  $('.js-submenu-btn').on('click', function () {	    var self = $(this),	      item = $('.sidebar__item'),	      list = $('.sidebar__submenu-list');	    if (self.closest('.sidebar__item').hasClass('active')) {	      list.slideUp('slow');	      item.removeClass('active');	    } else {	      list.slideUp('slow');	      item.removeClass('active');	      self.closest('.sidebar__item').toggleClass('active');	      self.closest('.sidebar__item').find('.sidebar__submenu-list').slideToggle('slow');	    }	  });	}	sidebarSubmenu();	function taskProgressbar() {	  $('.js-task-progressbar').progressbar({	    value: 80	  });	}	taskProgressbar();
	$('.js-slider-range').each(function() {		$(this).slider({			appendTo: '.js-slider-wrap',			value: $(this).data('value'),			min: $(this).data('min'),			max: $(this).data('max'),			create: function(event, ui) {				$(this).closest('.js-slider-wrap').find('.js-value').text($(this).slider('value'));			},			slide: function(event, ui) {				$(this).closest('.js-slider-wrap').find('.js-value').text(ui.value);			}		});	})	$('.js-datetimepicker').datetimepicker({		dateFormat: "dd.mm.yy",		changeMonth: true,		changeYear: true,		monthNamesShort: ["January", "February", "March", "April", "May", "June", 		"July", "August", "September", "October", "November", "December"],		yearRange: "2018:2024",		timeText: 'Time',		hourText: 'Hour',		minuteText: 'Minute',		showSecond: false,		showButtonPanel: false,		stepMinute: 5,		hour: 9,		minute: 20	});	//---------------------------------------//	// Подключаем основные скрипты ( develop )	//---------------------------------------//	function customScroll() {		$('.page-main').mCustomScrollbar({			mouseWheel: {scrollAmount: 120, normalizeDelta: true},			keyboard: {enable: false}		});	}	customScroll();	function tabletSidebar() {		$('.js-sidebar-toggler').on('click', function() {			if ($(window).innerWidth() < 1023) {				console.log('click');				$(this).toggleClass('active');				$('.js-sidebar-collapse').toggleClass('show');			}		});	}	tabletSidebar();	function height() {		if ($('.js-height').length) {			$('.js-height').matchHeight({				byRow: true,				property: 'height',				target: null,				remove: false			});		}	}	height();	function tabs() {		var $wrap = $('.js-tabs-wrap');		if ($wrap.length) {			$wrap.each(function () {				var $that = $(this),				$tab = $that.find('.js-tab'),				$content = $that.find('.js-tab-content');				$tab.on('click', function (e) {					e.preventDefault();					var self = $(this),					index = self.index();					self.addClass('active').siblings().removeClass('active');					$content.eq(index).addClass('active').siblings().removeClass('active');				});			});		}	}	tabs();	function customSelect() {		$('.js-select').each(function() {			$(this).select2({				dropdownParent: $(this).closest('.js-select-wrap')			});		})	}	function customSelectSearch() {		function formatState(state) {			var baseUrl = $(state.element).data('img');			if (!baseUrl) {				return state.text			}			var $state = $(				'<span class="img-wrap"> <img src=" ' + baseUrl + ' "/> <span class="img-text">' + state.text + '</span></span>'				);			return $state;		};		$('.js-select-search').each(function() {			$(this).select2({				dropdownParent: $(this).closest('.js-select-wrap-search'),				templateResult: formatState,				templateSelection: formatState,				escapeMarkup: function (m) {					return m;				} 			});		})	}	customSelect();	customSelectSearch();	$('.js-select-search').on('select2:open', function (e) {		$('.select2-search input').prop('focus', 0);		$('.select2-search input').attr('placeholder', 'Поиск по каналам');		$('.select-link').appendTo('.select2-search--dropdown');	});	//progress bar for the task-card module	function taskProgressbar() {		$('.js-task-progressbar').each(function() {			var val =  $(this).data('val');			$(this).progressbar({				value: val			});		});	}	taskProgressbar();})