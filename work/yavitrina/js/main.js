function preloader() {	if ($('#preloader').length) {		setTimeout(function() {			$('#preloader').fadeOut('slow', function() {				$('body').removeClass('overflow-hidden').css('padding', '');			});		}, 1000);	}}$(window).on('load', preloader);//---------------------------------------//// Подключаем набор написаных скриптов (requare/developRequare.js)//---------------------------------------//// выполняем их после того, как DOM построится $(document).ready(function() {	 $.reject({  	//	header: 'Your browser is not supported here', // Header Text  	//	paragraph1: 'You are currently using an unsupported browser', // Paragraph 1  	//	paragraph2: 'Please install one of the many optional browsers below to proceed',  	//	closeMessage: 'Close this window at your own demise!' // Message below close window link  		 reject: { 			all: false,			msie: 9,			chrome: 40,			firefox: 30,			safari4: 4		}, 		display: [], // What browsers to display and their order (default set below)		browserShow: true, // Should the browser options be shown?		browserInfo: { // Settings for which browsers to display			chrome: {				// Text below the icon				text: 'Google Chrome',				// URL For icon/text link				url: 'http://www.google.com/chrome/'				// (Optional) Use "allow" to customized when to show this option				// Example: to show chrome only for IE users				// allow: { all: false, msie: true }			},			firefox: {				text: 'Mozilla Firefox',				url: 'http://www.mozilla.com/firefox/'			},			safari: {				text: 'Safari',				url: 'http://www.apple.com/safari/download/'			},			opera: {				text: 'Opera',				url: 'http://www.opera.com/download/'			},			msie: {				text: 'Microsoft Edge',				url: 'http://www.microsoft.com/'			}		},		// Pop-up Window Text		header: 'Did you know that your Internet Browser is out of date?',		paragraph1: 'Your browser is out of date, and may not be compatible with '+					'our website. A list of the most popular web browsers can be '+					'found below.',		paragraph2: 'Just click on the icons to get to the download page',		// Allow closing of window		close: true,		// Message displayed below closing link		closeMessage: 'By closing this window you acknowledge that your experience '+						'on this website may be degraded',		closeLink: 'Close This Window',		closeURL: '#',		// Allows closing of window with esc key		closeESC: true,		// Use cookies to remmember if window was closed previously?		closeCookie: false,		// Cookie settings are only used if closeCookie is true		cookieSettings: {			// Path for the cookie to be saved on			// Should be root domain in most cases			path: '/',			// Expiration Date (in seconds)			// 0 (default) means it ends with the current session			expires: 0		},		// Path where images are located		imagePath: './img/browsers/',		// Background color for overlay		overlayBgColor: '#000',		// Background transparency (0-1)		overlayOpacity: 0.8,		// Fade in time on open ('slow','medium','fast' or integer in ms)		fadeInTime: 'fast',		// Fade out time on close ('slow','medium','fast' or integer in ms)		fadeOutTime: 'fast',		// Google Analytics Link Tracking (Optional)		// Set to true to enable		// Note: Analytics tracking code must be added separately		analytics: false	}); 	//---------------------------------------//	// Подключаем скрипты компонентов страниц ( modules )	//---------------------------------------//	function rangePrice() {		$('.js-range').slider({			range: true,			min: $('.js-range').data('min'),			max: $('.js-range').data('max'),			values: $('.js-range').data('values'),			slide: function( event, ui ) {				$('.js-min-value').val(ui.values[0]);				$('.js-max-value').val(ui.values[1]);			}		});		$('.js-min-value').val($('.js-range').slider('values', 0));		$('.js-max-value').val($('.js-range').slider('values', 1));	}	rangePrice();	$('.js-scroll').mCustomScrollbar({		axis: 'y',		scrollbarPosition: 'outside'	});	function toggleFilter() {		$('.js-head').on('click', function() {			$(this).closest('.js-item').find('.js-content').slideToggle();		})	}	toggleFilter();
	function toggleImg() {		$('.js-item').on('click', function() {			var thisSrc = $(this).find('img').attr('src');			$(this).addClass('active').siblings().removeClass('active');			$('.js-img-big').attr('src', thisSrc);		})	}	toggleImg();
	function closeCookie() {		$('.js-close-cookie').on('click', function() {			$('.js-cookie').addClass('hide');		})	}	closeCookie();
	function autoFooter() {		var footer = $('.footer');		if ($('.footer').length) {			var wrapper = $('.wrapper');			var footerHeight = footer.outerHeight();			footer.css('margin-top', -footerHeight);			wrapper.css('padding-bottom', footerHeight);		}	}	setTimeout(autoFooter, 100);	$(window).on('resize', autoFooter);
	$('.js-slider-goods').slick({		dots: false,		infinite: true,		speed: 300,		slidesToShow: 4,		slidesToScroll: 1,		responsive: [		{			breakpoint: 1250,			settings: {				slidesToShow: 3			}		},		{			breakpoint: 850,			settings: {				slidesToShow: 2			}		},		{			breakpoint: 600,			settings: {				slidesToShow: 1			}		}		]	});
	//---------------------------------------//	// Подключаем основные скрипты ( develop )	//---------------------------------------//	function customSelect() {		$('.js-select-sort').select2({			dropdownParent: $('.js-select-wrap')		});		$('.js-select-city').select2({			dropdownParent: $('.js-select-wrap-city')		});		$('.js-city-btn').on('click', function(e) {			if ($('.js-select-wrap-city').hasClass('active')) {				$('.js-select-wrap-city').removeClass('active');				$('.js-select-city').select2('close');				$('body').removeClass('overflow');			} else {				$('.js-select-wrap-city').addClass('active');				$('.js-select-city').select2('open');				$('body').addClass('overflow');			}		})		$('.js-close-select').on('click', function(e) {			e.preventDefault();			$('.js-select-wrap-city').removeClass('active');			$('body').removeClass('overflow');		})		$('.js-select-city').on('select2:select select2:close', function (e) {			var selectedElement = $(e.currentTarget);			var selectVal = selectedElement.val();			$('.js-city-name').text(selectVal);			setTimeout(function() {				$('.js-select-wrap-city').removeClass('active');			}, 100)			// $('.js-select-city').select2('close');			$('body').removeClass('overflow');		});		$(document).on('click', function(e) {			if(!$(e.target).closest('.js-select-wrap-city').length && !$(e.target).closest('.js-city-btn').length)  {				$('.js-select-city').select2('close');				$('.js-select-wrap-city').removeClass('active');			}		})		$('.js-select-city').on('select2:open', function (e) {			$('.select2-search input').prop('focus', 0);			$('.select2-search input').attr('placeholder', 'Название города');			$('.select2-results__options').mCustomScrollbar('destroy');			$('.select__dropdown .select2-results__options').mCustomScrollbar('update')			setTimeout(function () {				$('.select2-results > .select2-results__options').mCustomScrollbar({					axis: "y"				});			}, 100);		});	}	customSelect();	function wave(idName, fillColor, fillBg, lineHeight, yHeight, levelHeight, minD, maxD, countNumbet) {		var c = document.getElementById(idName),		ctx = c.getContext('2d'),		cw = c.width = window.innerWidth,		ch = c.height = window.innerHeight,		points = [],		tick = 0,		opt = {			count: countNumbet,			range: {				x: 20,				y: yHeight			},			duration: {				min: minD,				max: maxD			},			thickness: lineHeight,			strokeColor: fillColor,			level: levelHeight,			curved: true		},		rand = function(min, max){			return Math.floor( (Math.random() * (max - min + 1) ) + min);		},		ease = function (t, b, c, d) {			if ((t/=d/2) < 1) return c/2*t*t + b;			return -c/2 * ((--t)*(t-2) - 1) + b;		};		ctx.lineJoin = 'round';		ctx.lineWidth = opt.thickness;		ctx.strokeStyle = opt.strokeColor;		var Point = function(config){			this.anchorX = config.x;			this.anchorY = config.y;			this.x = config.x;			this.y = config.y;			this.setTarget();  		};		Point.prototype.setTarget = function(){			this.initialX = this.x;			this.initialY = this.y;			this.targetX = this.anchorX + rand(0, opt.range.x * 2) - opt.range.x;			this.targetY = this.anchorY + rand(0, opt.range.y * 2) - opt.range.y;			this.tick = 0;			this.duration = rand(opt.duration.min, opt.duration.max);		}		Point.prototype.update = function(){			var dx = this.targetX - this.x;			var dy = this.targetY - this.y;			var dist = Math.sqrt(dx * dx + dy * dy);			if(Math.abs(dist) <= 0){				this.setTarget();			} else {				var t = this.tick;				var b = this.initialY;				var c = this.targetY - this.initialY;				var d = this.duration;				this.y = ease(t, b, c, d);				b = this.initialX;				c = this.targetX - this.initialX;				d = this.duration;				this.x = ease(t, b, c, d);				this.tick++;			}		};		Point.prototype.render = function(){			ctx.beginPath();			ctx.arc(this.x, this.y, 3, 0, Math.PI * 2, false);			ctx.fillStyle = '#fff';			ctx.fill();		};		var updatePoints = function(){			var i = points.length;			while(i--){				points[i].update();			}		};		var renderPoints = function(){			var i = points.length;			while(i--){				points[i].render();			}		};		var renderShape = function(){			ctx.beginPath();			var pointCount = points.length;			ctx.moveTo(points[0].x, points[0].y);	  			var i;			for (i = 0; i < pointCount - 1; i++) {				var c = (points[i].x + points[i + 1].x) / 2;				var d = (points[i].y + points[i + 1].y) / 2;				ctx.quadraticCurveTo(points[i].x, points[i].y, c, d);			}			ctx.lineTo(-opt.range.x - opt.thickness, ch + opt.thickness);			ctx.lineTo(cw + opt.range.x + opt.thickness, ch + opt.thickness);			ctx.closePath();   			ctx.fillStyle = fillBg;			ctx.fill();  			ctx.stroke();		};		var clear = function(){			ctx.clearRect(0, 0, cw, ch);		};		var loop = function(){			window.requestAnimFrame(loop, c);			tick++;			clear();			updatePoints();			renderShape();		};		var i = opt.count + 2;		var spacing = (cw + (opt.range.x * 2)) / (opt.count-1);		while(i--){			points.push(new Point({				x: (spacing * (i - 1)) - opt.range.x,				y: ch - (ch * opt.level)			}));		}		window.requestAnimFrame=function(){return window.requestAnimationFrame||window.webkitRequestAnimationFrame||window.mozRequestAnimationFrame||window.oRequestAnimationFrame||window.msRequestAnimationFrame||function(a){window.setTimeout(a,1E3/60)}}();		loop();	}	if (document.getElementById('c')) {		wave('c', '#fff', '#f8f8f8', 15, 80, .9, 140, 150, 3);	}	if (document.getElementById('c2')) {		wave('c2', '#fff', '#f8f8f8', 15, 80, .9, 140, 150, 3);	}	if (document.getElementById('c3')) {		wave('c3', '#fff', 'transparent', 70, 200, .7, 100, 120, 5);	}	if (document.getElementById('c4')) {		wave('c4', '#fff', '#fff', 15, 50, .85, 140, 150, 3);	}	if (document.getElementById('c5')) {		wave('c5', '#fff', '#fff', 15, 80, .55, 140, 150, 3);	}	if (document.getElementById('c6')) {		wave('c6', '#fff', '#fff', 15, 80, .85, 140, 150, 3);	}	if (document.getElementById('c7')) {		wave('c7', '#fff', '#f8f8f8', 25, 50, .9, 140, 150, 3);	}})